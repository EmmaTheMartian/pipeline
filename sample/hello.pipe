const greet = impure (person) {
    print("Hello, ");
    print(person);
    println("!");
};

const doTest = impure () {
    if (maybe()) {
        return "true";
    } else {
        return "false";
    };
    return "fail :(";
};

const name = {
    if (maybe()) {
        return "Emma";
    } else {
        return "Emmeline";
    };
};

const main = impure () {
    greet(doTest());
};
