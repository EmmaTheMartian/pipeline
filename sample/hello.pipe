const io = import("pipelib/io.pipe");

const li = list();
li.append("Hello, ");
li.append("World! ");
li.append("-From ");
li.append("Pipeline :D");

li.for_each(impure (it) {
	io.print(it);
});
io.println("");
